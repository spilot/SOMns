class Write usingPlatform: platform = Value ()
(
  public main: args = (
    | i slot |
    (* `i` is an ordinary counter variable.
       `slot` is a counter variable whose type gets changed to String after one iteration. *)
    i::     0.
    slot::  0.
    [ i < 4 ] whileTrue: [
      ( i = 1 ) ifTrue: [
        slot:: 'yo'.
      ].
      (* In the first iteration, this gets rewritten to a `IncrementOperationNode`.
         In the second iteration, it gets rewritten to the original subtree because
         the type of `slot` has changed. *)
      slot:: slot + 1.
      i:: i + 1.
    ].
    slot println.
    ^ 0
  )
)